<Window x:Class="QTop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QTop"
        Width="1180"
        Height="760"
        MinWidth="940"
        MinHeight="620"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource WindowBrush}"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        FontSize="13"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <Style x:Key="DetailLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="Margin" Value="0,0,0,1" />
        </Style>
        <Style x:Key="DetailTextBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5,2" />
            <Setter Property="MinHeight" Value="23" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Hidden" />
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="ToolTip" Value="{x:Null}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconOnlyButton" TargetType="Button">
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="Transparent" CornerRadius="3">
                            <Viewbox Width="13" Height="13" Stretch="Uniform">
                                <Canvas Width="16" Height="16">
                                    <Path Stroke="{TemplateBinding Foreground}" StrokeThickness="1.35" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M6.5,5.5 L12.5,5.5 L12.5,13.5 L4.5,13.5 L4.5,7.5" />
                                    <Path Stroke="{TemplateBinding Foreground}" StrokeThickness="1.35" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M3.5,10.5 L2.5,10.5 L2.5,2.5 L10.5,2.5 L10.5,3.5" />
                                </Canvas>
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource SoftAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CopyButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>

        <Style x:Key="TreeToggle" TargetType="ToggleButton">
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent">
                            <Path x:Name="Chevron"
                                  Data="M0,0 L4,4 L0,8"
                                  Stroke="{StaticResource MutedBrush}"
                                  StrokeThickness="1.4"
                                  StrokeLineJoin="Round"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0" />
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chevron" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chevron" Property="Stroke" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryPill" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Pill"
                                Background="White"
                                BorderBrush="{StaticResource CardBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10,5">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource SoftAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Pill" Property="Background" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="Pill" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearSearchButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="Transparent" CornerRadius="3">
                            <Path Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="1.4"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Data="M0,0 L7,7 M7,0 L0,7"
                                  Width="7"
                                  Height="7"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#E5E7EB" />
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Refresh" Click="RefreshNow_Click" />
                <Separator />
                <MenuItem Header="E_xit" Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Hide protected/system" IsCheckable="True" IsChecked="{Binding HideProtectedSystem, Mode=TwoWay}" />
                <MenuItem Header="_Confirm before kill" IsCheckable="True" IsChecked="{Binding ConfirmBeforeKill, Mode=TwoWay}" />
            </MenuItem>
            <MenuItem Header="_Process">
                <MenuItem Header="Copy _name" Click="CopyProcessName_Click" />
                <MenuItem Header="Copy _PID" Click="CopyPid_Click" />
                <MenuItem Header="Copy _path" Click="CopyPath_Click" />
                <Separator />
                <MenuItem Header="Open file _location" Click="OpenFileLocation_Click" />
                <MenuItem Header="Refresh _selected" Click="RefreshSelectedProcess_Click" />
                <Separator />
                <MenuItem Header="_Kill process" Click="KillProcess_Click" InputGestureText="Del" Foreground="{StaticResource DangerBrush}" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About QTop" Click="AboutMenuItem_Click" />
            </MenuItem>
        </Menu>

        <ToolBar DockPanel.Dock="Top" Band="0" BandIndex="0" Background="#F9FAFB" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <ToolBar.Resources>
                <Style TargetType="{x:Type ToolBar}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ToolBar}">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ToolBarPanel IsItemsHost="True" Margin="6,3" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ToolBar.Resources>
            <Label Content="Search:" VerticalAlignment="Center" />
            <Grid Width="260" VerticalAlignment="Center">
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource EntryBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=250}"
                         Padding="9,6,26,6"
                         ToolTip="Focus: F3 or Ctrl+F — clear: Esc"
                         PreviewKeyDown="SearchBox_PreviewKeyDown" />
                <Button Style="{StaticResource ClearSearchButton}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,5,0"
                        ToolTip="Clear search (Esc)"
                        Click="ClearSearch_Click" />
                <TextBlock Text="Process name, PID, path, service" Foreground="#9CA3AF" Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <CheckBox Content="Hide protected/system" IsChecked="{Binding HideProtectedSystem, Mode=TwoWay}" VerticalAlignment="Center" Margin="16,0,0,0" />
            <Label Content="Refresh:" VerticalAlignment="Center" Margin="16,0,0,0" />
            <ComboBox x:Name="RefreshIntervalCombo"
                      Style="{StaticResource ComboBoxInput}"
                      ItemsSource="{Binding RefreshIntervals}"
                      DisplayMemberPath="DisplayName"
                      SelectedItem="{Binding SelectedRefreshInterval, Mode=TwoWay}"
                      SelectionChanged="RefreshIntervalCombo_SelectionChanged" />
            <Button Style="{StaticResource FlatButton}" Content="Refresh" Margin="10,0,0,0" Click="RefreshNow_Click" />
        </ToolBar>

        <Border DockPanel.Dock="Bottom" Height="28" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,1,0,0" Background="#F9FAFB">
            <StatusBar Background="Transparent" VerticalAlignment="Stretch">
                <StatusBarItem Width="82" Padding="4,0" HorizontalContentAlignment="Left">
                    <Border Padding="6,2" CornerRadius="4" HorizontalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#FEE2E2" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsElevated}" Value="True">
                                        <Setter Property="Background" Value="#DCFCE7" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding ElevationStatusText}" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#991B1B" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsElevated}" Value="True">
                                            <Setter Property="Foreground" Value="#166534" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </StatusBarItem>
                <Separator />
                <StatusBarItem Width="135">
                    <TextBlock Text="{Binding ProcessCountStatusText}" TextTrimming="CharacterEllipsis" />
                </StatusBarItem>
                <Separator />
                <StatusBarItem Width="170">
                    <TextBlock Text="{Binding LastRefreshStatusText}" TextTrimming="CharacterEllipsis" />
                </StatusBarItem>
                <Separator />
                <StatusBarItem Width="120">
                    <TextBlock Text="{Binding RefreshStatusText}" TextTrimming="CharacterEllipsis" />
                </StatusBarItem>
                <Separator />
                <StatusBarItem Width="220">
                    <TextBlock Text="{Binding FilterStatusText}" TextTrimming="CharacterEllipsis" />
                </StatusBarItem>
                <Separator />
                <StatusBarItem>
                    <Grid MinWidth="260">
                        <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasAlert}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding AlertText}" Foreground="OrangeRed" FontWeight="SemiBold" TextTrimming="CharacterEllipsis">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasAlert}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </StatusBarItem>
            </StatusBar>
        </Border>

        <Grid Margin="12,10,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0"
                     ItemsSource="{Binding CategoryFilters}"
                     SelectedItem="{Binding SelectedCategoryFilter, Mode=TwoWay}"
                     ItemContainerStyle="{StaticResource CategoryPill}"
                     Background="Transparent"
                     BorderThickness="0"
                     Margin="0,0,0,10"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding StatsText}" Margin="7,0,0,0" Opacity="0.7" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="4" />
                        <ColumnDefinition Width="330" />
                    </Grid.ColumnDefinitions>

                    <DataGrid x:Name="ProcessGrid"
                              Grid.Column="0"
                              Style="{StaticResource ProcessGrid}"
                              ItemsSource="{Binding Processes}"
                              SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                              CanUserSortColumns="False"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              PreviewKeyDown="ProcessGrid_PreviewKeyDown">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Process" Width="280" MinWidth="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="{Binding IndentMargin}">
                                            <ToggleButton IsChecked="{Binding IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <ToggleButton.Style>
                                                    <Style TargetType="ToggleButton" BasedOn="{StaticResource TreeToggle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasVisibleChildren}" Value="False">
                                                                <Setter Property="Visibility" Value="Hidden" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ToggleButton.Style>
                                            </ToggleButton>
                                            <Image Source="{Binding Icon}" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0" />
                                            <TextBlock Text="{Binding ProcessName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="64" ElementStyle="{StaticResource NumberCellText}" />
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryLabel}" Width="116" />
                            <DataGridTextColumn Header="CPU %" Binding="{Binding CpuPercentText}" Width="70" ElementStyle="{StaticResource NumberCellText}" />
                            <DataGridTextColumn Header="CPU time" Binding="{Binding CpuTimeText}" Width="88" ElementStyle="{StaticResource NumberCellText}" />
                            <DataGridTextColumn Header="Memory" Binding="{Binding MemoryText}" Width="96" ElementStyle="{StaticResource NumberCellText}" />
                            <DataGridTextColumn Header="Count" Binding="{Binding NameGroupCountText}" Width="58" ElementStyle="{StaticResource NumberCellText}" />
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Copy process name" Click="CopyProcessName_Click" />
                                <MenuItem Header="Copy PID" Click="CopyPid_Click" />
                                <MenuItem Header="Copy executable path" Click="CopyPath_Click" />
                                <Separator />
                                <MenuItem Header="Open file location" Click="OpenFileLocation_Click" />
                                <MenuItem Header="Refresh selected" Click="RefreshSelectedProcess_Click" />
                                <Separator />
                                <MenuItem Header="Kill process" Click="KillProcess_Click" InputGestureText="Del" Foreground="{StaticResource DangerBrush}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>

                    <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource CardBorderBrush}" />

                    <Border Grid.Column="2" BorderThickness="1,0,0,0" BorderBrush="{StaticResource CardBorderBrush}">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Bottom" Margin="12,8,12,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Style="{StaticResource CopyButton}" Margin="0" Content="Open location" Click="OpenFileLocation_Click" />
                                    <Button Grid.Column="2" Style="{StaticResource CopyButton}" Margin="0" Content="Refresh" Click="RefreshSelectedProcess_Click" />
                                </Grid>
                                <Button Style="{StaticResource DangerButton}" Content="Kill process" Click="KillProcess_Click" IsEnabled="{Binding CanKillSelected}" ToolTip="Del" Padding="6,4" FontSize="11" Margin="0,8,0,0" />
                                <CheckBox Content="Confirm before kill" IsChecked="{Binding ConfirmBeforeKill, Mode=TwoWay}" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,0" HorizontalAlignment="Center" />
                            </StackPanel>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="12,10">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding SelectedProcessTitle}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis" MaxWidth="250" />
                                        <Button Style="{StaticResource IconOnlyButton}" ToolTip="Copy process name" Click="CopyProcessName_Click" />
                                    </StackPanel>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="PID" Style="{StaticResource DetailLabel}" />
                                                <Button Style="{StaticResource IconOnlyButton}" ToolTip="Copy PID" Click="CopyPid_Click" />
                                            </StackPanel>
                                            <TextBox Text="{Binding SelectedProcess.ProcessId, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="0" Grid.Column="2">
                                            <TextBlock Text="Parent" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.ParentProcessIdText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="2" Grid.Column="0">
                                            <TextBlock Text="Category" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.CategoryLabel, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="2">
                                            <TextBlock Text="Access" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.AccessState, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="4" Grid.Column="0">
                                            <TextBlock Text="Session" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.UserSession, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="4" Grid.Column="2">
                                            <TextBlock Text="Integrity" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.IntegrityText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="6" Grid.Column="0">
                                            <TextBlock Text="CPU" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.CpuDetailText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="6" Grid.Column="2">
                                            <TextBlock Text="Memory" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.MemoryText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,4,0,8" />
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0">
                                            <TextBlock Text="Threads" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.ThreadCountText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="0" Grid.Column="2">
                                            <TextBlock Text="Handles" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.HandleCountText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
                                            <TextBlock Text="Started" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.StartTimeText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,4,0,8" />
                                    <StackPanel Margin="0,0,0,8">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Path" Style="{StaticResource DetailLabel}" />
                                            <Button Style="{StaticResource IconOnlyButton}" ToolTip="Copy executable path" Click="CopyPath_Click" />
                                        </StackPanel>
                                        <TextBox Text="{Binding SelectedProcess.ExecutablePath, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="Command line" Style="{StaticResource DetailLabel}" />
                                        <TextBox Text="{Binding SelectedProcess.CommandLine, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                    </StackPanel>

                                    <Separator Margin="0,4,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0">
                                            <TextBlock Text="Company" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.CompanyName, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="0" Grid.Column="2">
                                            <TextBlock Text="Product" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.ProductName, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
                                            <TextBlock Text="Description" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.Description, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3">
                                            <TextBlock Text="Services" Style="{StaticResource DetailLabel}" />
                                            <TextBox Text="{Binding SelectedProcess.ServiceNamesText, TargetNullValue=Unavailable, Mode=OneWay}" Style="{StaticResource DetailTextBox}" />
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
